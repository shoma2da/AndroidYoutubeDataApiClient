dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "android-23"
    - echo y | android update sdk --no-ui --all --filter "build-tools-23.0.0"

test:
  override:
    - ./gradlew test unitTestCoverage
    - find . -name coverage.xml
